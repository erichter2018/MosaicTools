5.0.3
- Fix: Mosaic 2.0.4.4 compatibility — report editor (ProseMirror) and metadata scraping now work under the new Mosaic version while remaining compatible with 2.0.3
- Fix: CDP iframe connection now matches any radpair.com host (new Mosaic uses radpair.prod.aws.radpartners.com instead of rp.radpair.com)
- Fix: DRAFTED status detection works when Mosaic nests it as a child of "Current Study" (2.0.4.4) rather than a sibling (2.0.3)
- Fix: Process Report, Sign Report, Create Impression, and Discard Study buttons now work with 2.0.4.4's renamed/uppercased labels (PROCESS REPORT, SIGN FINAL, CUSTOM IMPRESSION, Discard Changes)
- Refactor: Mosaic current-study metadata extraction moved to the shared MosaicUIA library (used by both MosaicTools and RVUCounter)

5.0.2
- Fix: Critical findings name parser no longer mistakes Clario note status words ("Addendum Connected") for clinician names when followed by a credential like PA

5.0.1
- Fix: Clario priority/class extraction and Aidoc scraping no longer gated behind "Show Clinical History" checkbox — these now run independently so pipe broadcasts, stroke detection, and ignore-inpatient-drafted work even when the clinical history alert box is hidden

5.0.0
- Rename: all user-facing text now says "TBWU" instead of "RVU" (settings, metrics bar, Clario injection, tooltips)
- AssemblyAI upgraded to Universal-3 Pro Streaming (u3-rt-pro) with medical domain and 1000-term keyterm support
- Deepgram Flux Configure: update keyterms mid-stream without reconnecting
- New STT providers: ElevenLabs, Smallest AI
- Radiology keyterm database for STT provider boosting
- NoteFormatter: postfix credential matching ("Jane Smith RN") before prefix title matching
- Fix: CDP study change detection delayed up to 30s after idle — now wakes slow path immediately when CDP detects new accession

4.3.10
- Fix: dictation confidence colors (green/yellow/orange) now persist across multiple insertions instead of losing earlier word colors
- Fix: impression action buttons no longer corrupt report text when editing/deleting impression items (buttons moved outside ProseMirror DOM)
- Fix: impression delete icons (trash can) now visible inside ordered list items
- Fix: impression buttons reposition correctly on scroll/resize
- Fix: ruler OCR no longer misreads measurement annotations as tick marks (text region exclusion)
- Fix: viewport border detection no longer treats ruler lines as borders
- Fix: transcription overlay no longer loses per-word confidence colors on rapid updates (RTF rebuild)
- Tighter spacing for RVU values in Clario worklist header

4.3.9
- Voice commands: "new line" / "next line" starts a new line, "new paragraph" / "next paragraph" starts a new paragraph (inserts line breaks via CDP)
- Fix: spoken punctuation cleanup no longer strips newline characters from new line/paragraph commands

4.3.8
- Fix: CTA+Perfusion template mismatch false positive — "CT BRAIN PERFUSION CTA HEAD NECK" no longer flags against "CTA Head and Neck with Perfusion" (CTA subsumes standalone CT)
- Fix: STT punctuation leaking into transcript editor when "auto-punctuate final report only" is enabled — regex-based stripping of all non-decimal punctuation
- Fix: STT text loss when quickly restarting recording — cleanup task no longer clobbers paste flag when new recording is already active
- Ensemble mode: driver/anchor model is now selectable (Deepgram, Soniox, Speechmatics, AssemblyAI) — no longer hardcoded to Deepgram
- Ensemble mode: secondary provider dropdowns dynamically exclude the driver and each other to prevent duplicate selections
- Ensemble metrics popup labels now reflect the actual driver provider name

4.3.7
- Fix: highlight mode (No Highlight/Regular/Rainbow) now reliably persists across accession changes — user's choice no longer reverts to Regular on new studies
- Fix: impression fixer button text ("+no change", "=improved", etc.) no longer leaks into report popup display
- Fix: impression fixer buttons now show in report popup even when "deletable impression points" is disabled
- Fix: RadAI popup (classic mode) now auto-closes after inserting impression into report
- Fix: template mismatch detection improved for combined studies (CT+CTA) and perfusion protocols
- Fix: floating toolbar no longer flickers during ruler refresh when positioned away from rulers
- Rainbow mode in-editor colors boosted for better visibility on dark backgrounds
- Custom STT: "comparison none/none available" filtered out when multiple comparisons dictated

4.3.6
- Fix: macros not populating on drafted studies (dormancy blocked slow-path macro insertion until user toggled dictation)
- Fix: keyboard hook health checker reinstalling hook every 15s when user is mouse/mic-only (exponential backoff after 2 consecutive reinstalls without keyboard activity)

4.3.5
- Fix: macros and accession detection failing when CDP returns empty data — now falls through to FlaUI backup scrape
- Fix: keyboard hotkeys/toggle dying mid-session — hook now runs on dedicated thread with its own message pump (like AutoHotkey)
- Keyboard hook health check tightened: 5s interval / 15s threshold (was 15s / 30s)
- Keyboard hook proactively reinstalls on workstation unlock (session unlock detection)
- Highlight mode (No Highlight / Regular / Rainbow) now persists across accession changes

4.3.4
- Fix: ruler overlay and auto-measurement not working when InteleViewer rulers are orange instead of yellow (broadened color detection)
- Reduced log noise: viewport scanning no longer floods log with per-cycle cluster data

4.3.3
- Auto-measurement segmentation overhaul: adaptive region growing with bilateral filtering, gradient-based edge stopping, and Canny edge-snap refinement for more accurate lesion boundary detection

4.3.2
- In-editor highlight mode toggle: three small buttons (No Highlight / Regular / Rainbow) in bottom-right of report editor to control text highlighting
- No Highlight mode: hides all dictation highlights (Mosaic native and STT), works with visual enhancements
- Rainbow mode: color-coded correlation between findings and impression — matching pairs share the same body-part-category color, directly in the editor
- Rainbow highlights auto-update with 2s debounce as you edit the report

4.3.1
- Impression fixer buttons in Mosaic editor: quick-insert/replace buttons appear directly on the IMPRESSION heading in the report editor (new setting under Experimental)
- Fix: custom processing TASK instructions containing the word "impression" caused false IMPRESSION split, putting dictated findings into the impression section
- Fix: ruler overlays persisted on screen when InteleViewer was minimized

4.3.0
- Visual report enhancements overhaul: inline paragraph flow (all sentences in a subsection flow continuously), compact blank lines, FINDINGS/IMPRESSION major headers (larger, white, spaced), grey subsection headers, baseline-aligned header text
- Spellcheck disabled in report editor when visual enhancements active (prevents false underlines from display mode changes)
- Quad Compare cost display: dynamically updated estimated cost for all 4 selected models, updates on slot change
- Fix: scroll fix overlay covering Clario UI when MosaicTools loaded before a case was started

4.2.7
- Multi-provider LLM: support for Google Gemini, OpenAI GPT, Groq, and xAI Grok — each with independent API keys, model selection, and processing modes
- Quad Compare mode: fire up to 4 models in parallel, first result wins, click badges to swap between outputs
- Model racing (Dual/Triple modes): parallel LLM calls with background upgrades — fast lite model shows immediately, stronger model replaces it when ready
- Clickable model badges in editor title bar: show model name + response time, click to swap editor content, red glow warns of possibly dropped findings
- LLM output normalization: fuzzy header matching fixes typos (e.g. BLADER→BLADDER), enforces template section order, strips fabricated boilerplate sections
- Transcript coverage validation: warns (red badge border + tooltip) when LLM output may have dropped dictated findings
- Contrast and radiation dose extraction: dictated "contrast 100 mL Omnipaque" or "dose DLP 450" extracted from transcript and placed in dedicated sections
- Meta-instruction stripping: dictation commands like "please summarize and remove irrelevant" filtered before LLM processing
- Visual report enhancements option: Carolina blue change-tracking highlights, inline subsection headers, content text hierarchy
- Consistency checker fix: 1-to-1 measurement matching prevents false positives when multiple items share anatomy keywords
- STT transcript comparison popup: side-by-side view of per-provider transcripts from ensemble metrics
- Dynamic CSS top offset: column layout survives browser zoom changes via ResizeObserver
- Correlation fix: fallback when dictated highlight filter matches zero findings after badge swap

4.2.6
- Fix: Soniox BPE token splitting — words like "inferior" no longer pasted as "inf erior" when subword tokens arrive across WebSocket messages
- Debug menu added to main banner right-click (same options as clinical history popup)

4.2.5
- Extract clinical history and comparison from transcript: dictated prefix sections replace template fields instead of being swallowed by LLM
- Multiple comparisons combined when dictated separately
- Empty transcript after extraction skips LLM and uses template as-is
- Spoken punctuation fix: "period", "comma", etc. now converted to symbols even with auto-punctuate on (providers sometimes output them as literal words)
- Fix: confidence highlight colors wrong on subsequent STT pastes (StaticRanges rebuilt fresh on each insert)
- Fix: dictated text stuck in transcription overlay (ensemble merge timer race condition with direct-paste flag)

4.2.4
- LLM split flow for Lite models: findings-only LLM call + separate impression handling prevents Lite from adding unsolicited impression items
- LLM prompt restructured with XML tags for better small-model compliance
- Subsection routing hints: pulmonary findings go to LUNGS AND PLEURA (not HEART), etc.
- LLM formatting: one contiguous line per subsection, anti-paraphrase rules preserve radiologist's exact words
- Local impression formatting for short dictated impressions (multi-line support)

4.2.3
- LLM model selector redesign: Version + Tier dropdowns replace raw model ID list, with preview badges and per-study/per-shift cost estimates

4.2.2
- Measurement & laterality consistency checker: red pulsing alert when measurements differ between FINDINGS and IMPRESSION, wrong-side errors (left/right mismatch), or study description laterality conflicts
- Unit-aware measurement comparison (1.8 cm = 18 mm), anatomy synonym matching (renal/kidney), bilateral suppression for midline studies
- Study description laterality: flags "right metacarpal" when study says "LEFT HAND", suppresses when study includes bilateral components (AP pelvis, chest, etc.)
- Click-to-dismiss consistency alerts with per-key tracking, cleared on study change
- Consistency search terms merged into CDP flashing alert text
- LLM Process Report prompt improvements: better sentence boundary inference from unpunctuated transcripts, no content dropped, in-transcript formatting instructions supported, proper sentence capitalization/punctuation enforced, custom section creation (e.g., "RADIATION DOSE:")

4.2.1
- Per-word confidence highlighting in Clario editor: 3-tier color system (normal/medium/low) via CSS Custom Highlight API
- Per-provider accuracy tracking: compare Deepgram, Soniox, Speechmatics word output against signed reports (session + all-time)
- Faster PTT restart: secondary provider EndSession is fire-and-forget, paste drain moved to background
- Fix: ensemble false correction from stale secondary words (audio filter now uses word midpoint instead of EndTime)
- Fix: punctuation lost in ensemble merge (DG word array lacks punctuation; now tokenizes transcript string)
- Fix: confidence color bleeding into subsequent dictation (every word gets explicit color via per-word StaticRange)
- Fix: Critical Findings CDP scrape picking up "Opening exam(s) in VR..." status messages (now requires date/time or EXAM NOTE)

4.2.0
- Findings/Impression mismatch alert: yellow pulsing border when FINDINGS and IMPRESSION contradict (e.g., "no bowel obstruction" vs "bowel obstruction"), click to dismiss
- CDP flashing alert text: gender mismatch (red) and FIM mismatch (orange) flash matching text in the report editor until corrected
- STT dictation text highlighting: subtle background tint on dictated text via CSS Custom Highlight API to distinguish from template text
- Clario CDP setup wizard: one-click Chrome CDP shortcut creation with directory junction (preserves Chrome profile/cookies/logins)
- Clario CDP URL now configurable in settings (supports different Clario instances)
- CDP status dashboard in Experimental settings: live connection status for Mosaic and Clario CDP
- Ensemble precision validation fix: report snapshot preserved across study transitions (was lost when scrape timer overwrote with empty report)
- Fix: duplicate consecutive words in ensemble merge (e.g., "inflammation inflammation")
- Fix: report popup deleted impressions reappearing (3-second cooldown prevents scrape timer from pushing stale text)
- Clario CDP: FlaUI fully replaced — Priority/Class, exam notes, critical notes all via CDP

4.1.2
- Fix: ensemble merge using stale secondary data from previous utterance (wall-clock arrival time tracking replaces provider timestamps)
- Fix: all-time ensemble stats incrementing on every PTT stop instead of only on report sign
- Fix: Soniox reconnect race crash (added connect lock to prevent disposed-object failures)
- Fix: timer CTS leak on expiry path
- Fix: hardcoded "SM" log label now uses actual provider name

4.1.1
- Soniox STT provider: new secondary provider for ensemble mode (BPE token merging for accurate word-level output)
- Ensemble correction validation: corrections verified against signed reports, precision tracked per-session and all-time
- Ensemble metrics popup: precision display showing correction accuracy (session + all-time)
- Ensemble trust fixes: stricter context-anchor requirements prevent false corrections on short phrases
- Ensemble: punctuation-only differences no longer counted as corrections (keeps Deepgram's smart formatting)
- Ensemble: all-time stats only update when report is signed (not during dictation)
- Deepgram: added utterance_end_ms for faster end-of-speech detection
- Soniox: cross-response BPE word merging (e.g. "perip"+"ancreatic" → "peripancreatic")
- Soniox: endpoint delay reduced to 300ms to match Deepgram timing

4.1.0
- STT Ensemble Mode: run Deepgram + AssemblyAI + Speechmatics simultaneously, secondaries correct low-confidence words via 2-of-3 voting
- Live ensemble metrics popup: draggable overlay showing confidence, per-provider corrections/confirms, consensus stats
- Per-provider keyterm auto-learning: each provider maintains its own learned keyterm list (separate JSON files)
- AssemblyAI: edge endpoint, faster endpointing, keyterm support via constructor
- Speechmatics: halved final latency (max_delay 2→1s), keyterm support via additional_vocab
- Keyterms now sent to all providers (previously Deepgram only)
- STT text processing extracted to shared SttTextProcessor (ensemble + single-provider use same pipeline)
- CDP: auto-scroll to cursor during dictation, hide report drag/delete handles (new settings)
- Fix: auto-fix clinical history pasting "TEMPLATE MISMATCH" diagnostic text into report instead of actual clinical history
- Fix: auto-fix infinite loop when template mismatch + empty accession caused endless 5-second paste cycles
- Fix: auto-fix max 2 paste attempts per study as safety net against infinite loops

4.0.0
- CDP integration: Chrome DevTools Protocol for direct Mosaic DOM access (faster, no COM leaks)
- Independent column scrolling: Transcript, Report, and Utility columns scroll separately instead of the whole page
- Scroll fix auto-reinjects on study changes — works seamlessly across 300+ studies per session
- CDP setting and scroll toggle in Experimental settings (requires Mosaic restart on first enable)
- CDP falls back to UI Automation when unavailable
